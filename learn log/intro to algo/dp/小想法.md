动态规划很有趣，它适用于解决很多复杂问题，例如最优路径，背包问题等等。
它的核心思想在于用空间换时间，存储之前情况的结果，然后在计算后续情况时直接调用。
解决一个动态规划问题的关键就在于找到状态转移方程，这个过程有点类似于寻找数学中的递归公式。

一般来说创建一个dp数组往往是二维的，因为有一些dp问题的状态有两个，我们会很自然地想到优化空间，
首先是滚动数组，尝试只保存必要的数据，不断用新数据替换旧数据的位置可以优化空间复杂度，其次，还可以进一步尝试直接将二维数组转变为一维数组，但我还没有研究，所以言尽于此。
